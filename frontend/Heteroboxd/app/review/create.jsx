import { StyleSheet, Text, View } from 'react-native'

const CreateReview = () => {
  //only serves the purpose to create a review from scratch, in the rare case a user click on review this film w/out previously rating
  return (
    <View>
      <Text>CreateReview</Text>
    </View>
  )
}

export default CreateReview

const styles = StyleSheet.create({})