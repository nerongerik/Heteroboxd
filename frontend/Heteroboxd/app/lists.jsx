import { StyleSheet, Text, View } from 'react-native'

// CONTEXT AWARE! User's created lists, or lists featuring film (tabular?)? TBD

const Lists = () => {
  return (
    <View>
      <Text>Lists</Text>
    </View>
  )
}

export default Lists

const styles = StyleSheet.create({})